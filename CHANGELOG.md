# Changelog

Entries below the first release are added automatically by
`.github/workflows/release.yml` release notes, not maintained by hand here.

## Unreleased

- Initial `dev` → `main` branch flow and release automation
  (`.github/workflows/release.yml`): signed APK/AAB build, auto-incremented
  versioning, and GitHub Release publishing on merge. See
  [docs/release.md](docs/release.md).
